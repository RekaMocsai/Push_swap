


void	rx(t_stack *stack,
		const char *name,
		t_command_list *list);

void	rr(t_stack *a,
		t_stack *b,
		const char *name,
		t_command_list *list);

void	rrx(t_stack *stack,
		const char *name,
		t_command_list *list);

void	rrr(t_stack *a,
		t_stack *b,
		const char *name,
		t_command_list *list);

void	px(t_stack *to,
		t_stack *from,
		const char *name,
		t_command_list *list);

void	sx(t_stack *stack,
		const char *name,
		t_command_list *list);

void	ss(t_stack *a,
		t_stack *b,
		const char *name,
		t_command_list *list);
